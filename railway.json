{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd content-quality-checker && python -m uvicorn main:app --host 0.0.0.0 --port 8500 & streamlit run app.py --server.port 8000 --server.address 0.0.0.0",
    "restartPolicyType": "ON_FAILURE"
  }
}
